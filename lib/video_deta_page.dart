import 'package:flutter/material.dart';

class VideoDataPage extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Center(
        child: Container(
          child: const Text('動画'),
        ),
      ),
    );
  }
}